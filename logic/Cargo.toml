[package]
name = "logic"
version = "0.1.0"
edition = "2024"

[dependencies]
embassy-time = { version = "0.4.0", features = ["defmt"]}
defmt = "0.3"

[dev-dependencies]
embassy-time = { version = "0.4.0", features = [ "std" ]}
